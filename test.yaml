apiVersion: batch/v1
kind: Job
metadata:
  name: test
spec:
  template:
    spec:
      affinity:
       nodeAffinity:
         requiredDuringSchedulingIgnoredDuringExecution:
           nodeSelectorTerms:
           - matchExpressions:
             - key: topology.kubernetes.io/region
               operator: In
               values:
               - us-west
             - key: gpu-type
               operator: In
               values:
               - 2080Ti
               - 1080Ti
      nodeSelector:
        nautilus.io/disktype: nvme
      containers:
      - name: test
        image: hurunyan/cuda10
        command:
        - sh
        - -c
        - "apt update && apt install -y python3-pip && pip3 install --upgrade pip && https://github.com/ClementPinard/Pytorch-Correlation-extension.git && python3 ./Pytorch-Correlation-extension/setup.py && cp /ceph/kitti2012.tar /mnt/data/ && tar -C /mnt/data/ -xvf /mnt/data/kitti2012.tar && cd /opt/repo/motionflow/ && chmod u+x ./scripts/train_baseline_joint.sh && ./scripts/train_baseline_joint.sh"
        volumeMounts:
        - name: data
          mountPath: /mnt/data
        - name: git-repo
          mountPath: /opt/repo
        - name: sceneflow-datasets
          mountPath: /ceph
        - name: dshm
          mountPath: /dev/shm
        resources:
          limits:
            memory: 45Gi
            cpu: "5"
            nvidia.com/gpu: "2"
            ephemeral-storage: 100Gi
          requests:
            memory: 36Gi 
            cpu: "4"
            nvidia.com/gpu: "2"
            ephemeral-storage: 100Gi
      initContainers:
      - name: init-clone-repo
        image: alpine/git
        args:
          - clone
          - --single-branch
          - https://github.com/shreyas23/motionflow.git
          - /opt/repo/motionflow
        volumeMounts:
          - name: git-repo
            mountPath: /opt/repo
      volumes:
      - name: data
        emptyDir: {}
      - name: git-repo
        emptyDir: {}
      - name: sceneflow-datasets
        persistentVolumeClaim:
          claimName: sceneflow-datasets
      - name: dshm
        emptyDir:
          medium: Memory
      restartPolicy: Never
  backoffLimit: 0
