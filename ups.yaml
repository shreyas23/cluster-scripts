apiVersion: batch/v1
kind: Job
metadata:
  name: ups
spec:
  template:
    spec:
      affinity:
       nodeAffinity:
         requiredDuringSchedulingIgnoredDuringExecution:
           nodeSelectorTerms:
           - matchExpressions:
             - key: gpu-type
               operator: In
               values:
               - 1080Ti
               - 2080Ti
      containers:
      - name: ups
        image: pytorch/pytorch:0.4.1-cuda9-cudnn7-devel
        command:
        - sh
        - -c
        - "export CUDA_HOME=/usr/local/cuda &&  export LD_LIBRARY_PATH=${CUDA_HOME}/lib64 && export PATH=${CUDA_HOME}/bin:${PATH} && apt update && apt install -y python3-pip tmux vim curl git unzip ffmpeg libsm6 libxext6 && python3 -m pip install --upgrade pip &&  mkdir -p /mnt/data/cityscapes/ && cd /mnt/data/cityscapes/ && cp -r /ceph/cityscapes/gtFine ./ && cp -r /ceph/cityscapes/leftImg8bit ./ && cd /opt/repo/UPSNet && git fetch && git checkout rollback && ln -s $CITYSCAPES_ROOT /mnt/data/cityscapes && chmod u+x init_cityscapes.sh && chmod u+x init.sh && ./init_cityscapes.sh && ./init.sh && python3 ./upsnet/upsnet_end2end_train.py --cfg upsnet/experiments/upsnet_resnet50_cityscapes_4gpu.yaml"
        volumeMounts:
        - name: data
          mountPath: /mnt/data
        - name: git-repo
          mountPath: /opt/repo
        - name: sceneflow-datasets
          mountPath: /ceph
        - name: dshm
          mountPath: /dev/shm
        resources:
          limits:
            memory: 40Gi
            cpu: "4"
            nvidia.com/gpu: "4"
            ephemeral-storage: 200Gi
          requests:
            memory: 30Gi 
            cpu: "3"
            nvidia.com/gpu: "4"
            ephemeral-storage: 200Gi
      initContainers:
      - name: init-clone-repo
        image: alpine/git
        args:
          - clone
          - https://github.com/arition/UPSNet.git
          - /opt/repo/UPSNet
        volumeMounts:
          - name: git-repo
            mountPath: /opt/repo
      volumes:
      - name: data
        emptyDir: {}
      - name: git-repo
        emptyDir: {}
      - name: sceneflow-datasets
        persistentVolumeClaim:
          claimName: sceneflow-datasets
      - name: dshm
        emptyDir:
          medium: Memory
      restartPolicy: "Never"
  backoffLimit: 0
